# 使い方
## 設定
- メインプログラム(Fibonacci_Dijkstra.pde)の3~13行目に記述されているパラメータをお好みで変更してください。
- `boolean diagonal` : 斜め移動の可否を設定します。`true`なら斜め移動可、`false`なら縦横のみの移動に制限します。
- `boolean aSter` : `true`ならA*アルゴリズムを使用し、`false`ならDijkstra法を用いて経路探索を行います。
- `boolean generateMaze` : 迷路生成を行うか、障害物を手書きするかを選べます。
